{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "SKIP_DB_MIGRATE": "true"
  },
  "build": {
    "env": {
      "SKIP_DB_MIGRATE": "true"
    }
  }
}